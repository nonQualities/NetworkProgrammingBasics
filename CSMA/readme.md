# Readme:
This is a basic network algorithm implementations in different languages, which doesn't reflect anything but my mood. Till now, the most complicated thing here would be a CSMA Algorithm in C#

## CSMA in C#
- CSMA or Carrier Sense Multiple Access is a step up from ALOHA. 
- It uses a expotential backoff period algorithm, to reduce collusions
- there are different kinds of CSMA as:
  - p-persistant
  - n-persitant
  - o-persistant etc
- 